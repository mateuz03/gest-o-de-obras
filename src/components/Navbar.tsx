/**
 * Compatibilidade temporaria:
 * o cabecalho global agora e renderizado pelo App Shell.
 * As telas antigas ainda podem importar este componente sem duplicar navegacao.
 */
export default function Navbar() {
  return null;
}
